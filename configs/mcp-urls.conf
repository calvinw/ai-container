# Shared MCP endpoint list. Add one MCP per line in name=url format.
# All setup scripts read this file — adding a line here registers the MCP in every AI tool.
# Example:
# dolt=https://your-mcp-server.example.com/sse
